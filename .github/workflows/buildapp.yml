name: Docker Image CI

on:
  pull_request:
    branches: [ "main" ]
    paths:
    - 'app/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Login into ECR
      run: |
        export DOCKER_IMAGE_TAG=$(date +%s)
        aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com

    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: cd app; docker build . --file Dockerfile --tag my-birthday-app:$DOCKER_IMAGE_TAG

    - name: Tag Docker image
      run: docker tag my-birthday-app-dev:$DOCKER_IMAGE_TAG ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com/my-birthday-app-dev:$DOCKER_IMAGE_TAG

    - name: Tag Docker image
      run: docker push ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com/my-birthday-app-dev:$DOCKER_IMAGE_TAG