apiVersion: v1
kind: Service
metadata:
  name: {{ include "birthdayapp.fullname" . }}
  labels:
    {{- include "birthdayapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "birthdayapp.selectorLabels" . | nindent 4 }}
